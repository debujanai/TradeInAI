/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    swcMinify: true,
    images: {
      domains: ['93.127.216.72'],
    },

  };
  
  module.exports = nextConfig;